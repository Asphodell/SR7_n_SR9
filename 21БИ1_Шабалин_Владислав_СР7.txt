алг СуммаЦифрЦелогоЧислаЕслиЦифрыНеЧётные
цел n
вещ a, s
начало
попытка
    ввод n 
искл 
    если n - [n] != 0
        вывод ('Ошибка, введите целое число')

функция СУММ_ЦИФР_В_ЧИСЛЕ(n)
    s := 0 
    a := 0
    пока n > 0 
        нц 	
            a = остат(n, 10)
            n = цел(n, 10)
	    если остат(а, 2) = 1
   	        s = s + a
	кц
    вернуть s

иначе		
    если n != 0
        если n < 0
	    n = n * -1
        вывод ('Сумма всех цифр числа -', вызовфункции(СУММ_ЦИФР_В_ЧИСЛЕ(n))
    иначе
        вывод ('Сумма всех цифр числа равна нулю.')
конец
